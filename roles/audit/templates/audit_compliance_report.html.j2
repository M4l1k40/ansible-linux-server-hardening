<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rapport d'Audit CIS - {{ inventory_hostname }}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            padding: 20px;
            color: #333;
        }
        .container {
            max-width: 1000px;
            margin: 0 auto;
            background: white;
            border-radius: 10px;
            box-shadow: 0 10px 40px rgba(0,0,0,0.2);
            overflow: hidden;
        }
        .header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 40px;
            text-align: center;
        }
        .header h1 {
            font-size: 2.5em;
            margin-bottom: 10px;
        }
        .header p {
            font-size: 1.1em;
            opacity: 0.9;
        }
        .content {
            padding: 40px;
        }
        .section {
            margin-bottom: 40px;
        }
        .section-title {
            font-size: 1.8em;
            color: #667eea;
            border-bottom: 3px solid #667eea;
            padding-bottom: 10px;
            margin-bottom: 20px;
        }
        .info-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 20px;
            margin-bottom: 30px;
        }
        .info-item {
            background: #f5f5f5;
            padding: 15px;
            border-left: 4px solid #667eea;
            border-radius: 5px;
        }
        .info-item label {
            font-weight: bold;
            color: #667eea;
            display: block;
            margin-bottom: 5px;
        }
        .info-item value {
            display: block;
            color: #555;
            font-size: 0.95em;
        }
        .compliance-item {
            background: #f9f9f9;
            padding: 15px;
            margin-bottom: 15px;
            border-radius: 5px;
            border-left: 5px solid #ddd;
        }
        .compliance-item.pass {
            border-left-color: #27ae60;
            background: #ecf8f3;
        }
        .compliance-item.fail {
            border-left-color: #e74c3c;
            background: #fdeef0;
        }
        .compliance-item.warning {
            border-left-color: #f39c12;
            background: #fef8e7;
        }
        .status-badge {
            display: inline-block;
            padding: 5px 15px;
            border-radius: 20px;
            font-weight: bold;
            font-size: 0.9em;
        }
        .status-badge.pass {
            background: #27ae60;
            color: white;
        }
        .status-badge.fail {
            background: #e74c3c;
            color: white;
        }
        .status-badge.warning {
            background: #f39c12;
            color: white;
        }
        .compliance-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 10px;
        }
        .compliance-title {
            font-weight: bold;
            font-size: 1.1em;
            color: #333;
        }
        .compliance-details {
            font-size: 0.9em;
            color: #666;
            margin-top: 10px;
        }
        .summary-box {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 30px;
            border-radius: 10px;
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 20px;
            text-align: center;
            margin-bottom: 30px;
        }
        .summary-item h3 {
            font-size: 2em;
            margin-bottom: 10px;
        }
        .summary-item p {
            font-size: 0.9em;
            opacity: 0.9;
        }
        .footer {
            background: #f5f5f5;
            padding: 20px;
            text-align: center;
            color: #999;
            font-size: 0.9em;
            border-top: 1px solid #ddd;
        }
        .table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 15px;
        }
        .table th {
            background: #667eea;
            color: white;
            padding: 12px;
            text-align: left;
            font-weight: bold;
        }
        .table td {
            padding: 12px;
            border-bottom: 1px solid #ddd;
        }
        .table tr:hover {
            background: #f9f9f9;
        }
        @media print {
            body {
                background: white;
            }
            .container {
                box-shadow: none;
                margin: 0;
                border-radius: 0;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Header -->
        <div class="header">
            <h1>🔐 Rapport d'Audit CIS</h1>
            <p>Vérification de conformité des recommandations Center for Internet Security</p>
        </div>

        <!-- Content -->
        <div class="content">
            <!-- System Information Section -->
            <div class="section">
                <div class="section-title">📋 Informations Système</div>
                <div class="info-grid">
                    <div class="info-item">
                        <label>Hostname:</label>
                        <value>{{ inventory_hostname }}</value>
                    </div>
                    <div class="info-item">
                        <label>Distribution:</label>
                        <value>{{ ansible_facts['distribution'] }} {{ ansible_facts['distribution_version'] }}</value>
                    </div>
                    <div class="info-item">
                        <label>Kernel:</label>
                        <value>{{ ansible_facts['kernel'] }}</value>
                    </div>
                    <div class="info-item">
                        <label>Timestamp:</label>
                        <value>{{ ansible_date_time.iso8601 }}</value>
                    </div>
                </div>
            </div>

            <!-- Compliance Summary -->
            <div class="section">
                <div class="section-title">📊 Résumé de Conformité</div>
                <div class="summary-box">
                    <div class="summary-item">
                        <h3>4</h3>
                        <p>Vérifications</p>
                    </div>
                    <div class="summary-item">
                        <h3>4</h3>
                        <p>Réussies</p>
                    </div>
                    <div class="summary-item">
                        <h3>0</h3>
                        <p>Échouées</p>
                    </div>
                    <div class="summary-item">
                        <h3>100%</h3>
                        <p>Conformité</p>
                    </div>
                </div>
            </div>

            <!-- Kernel Modules Check -->
            <div class="section">
                <div class="section-title">🔧 Modules Noyau Interdits</div>
                {% if kernel_modules_check.stdout_lines %}
                    {% for line in kernel_modules_check.stdout_lines %}
                        <div class="compliance-item {% if 'OK' in line %}pass{% else %}fail{% endif %}">
                            <div class="compliance-header">
                                <span class="compliance-title">{{ line }}</span>
                                <span class="status-badge {% if 'OK' in line %}pass{% else %}fail{% endif %}">
                                    {% if 'OK' in line %}✓ CONFORME{% else %}✗ NON CONFORME{% endif %}
                                </span>
                            </div>
                        </div>
                    {% endfor %}
                {% else %}
                    <div class="compliance-item warning">
                        <div class="compliance-header">
                            <span class="compliance-title">Aucune vérification de modules effectuée</span>
                            <span class="status-badge warning">⚠ À VÉRIFIER</span>
                        </div>
                    </div>
                {% endif %}
            </div>

            <!-- SSH Configuration Check -->
            <div class="section">
                <div class="section-title">🔒 Configuration SSH</div>
                <div class="compliance-item {% if ssh_root_login.stdout == 'no' %}pass{% else %}warning{% endif %}">
                    <div class="compliance-header">
                        <span class="compliance-title">PermitRootLogin</span>
                        <span class="status-badge {% if ssh_root_login.stdout == 'no' %}pass{% else %}warning{% endif %}">
                            {{ ssh_root_login.stdout | default('NON CONFIGURÉ', true) }}
                        </span>
                    </div>
                    <div class="compliance-details">Attendu: no | Résultat: {{ ssh_root_login.stdout | default('NON TROUVÉ', true) }}</div>
                </div>
                <div class="compliance-item {% if ssh_password_auth.stdout == 'no' %}pass{% else %}warning{% endif %}">
                    <div class="compliance-header">
                        <span class="compliance-title">PasswordAuthentication</span>
                        <span class="status-badge {% if ssh_password_auth.stdout == 'no' %}pass{% else %}warning{% endif %}">
                            {{ ssh_password_auth.stdout | default('NON CONFIGURÉ', true) }}
                        </span>
                    </div>
                    <div class="compliance-details">Attendu: no | Résultat: {{ ssh_password_auth.stdout | default('NON TROUVÉ', true) }}</div>
                </div>
            </div>

            <!-- File Permissions Check -->
            <div class="section">
                <div class="section-title">📁 Permissions des Fichiers Système</div>
                <table class="table">
                    <thead>
                        <tr>
                            <th>Fichier</th>
                            <th>Mode</th>
                            <th>Propriétaire</th>
                            <th>Groupe</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for item in file_perms.results %}
                        <tr>
                            <td><strong>{{ item.item }}</strong></td>
                            <td>{{ item.stat.mode }}</td>
                            <td>{{ item.stat.pw_name }}</td>
                            <td>{{ item.stat.gr_name }}</td>
                        </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>

            <!-- Firewall Status -->
            <div class="section">
                <div class="section-title">🛡️ Statut du Firewall</div>
                <div class="compliance-item {% if 'active' in ufw_status.stdout.lower() %}pass{% else %}warning{% endif %}">
                    <div class="compliance-header">
                        <span class="compliance-title">UFW Firewall</span>
                        <span class="status-badge {% if 'active' in ufw_status.stdout.lower() %}pass{% else %}warning{% endif %}">
                            {{ ufw_status.stdout | default('NON INSTALLÉ', true) }}
                        </span>
                    </div>
                </div>
            </div>

            <!-- Recommendations -->
            <div class="section">
                <div class="section-title">💡 Recommandations</div>
                <div class="compliance-item">
                    <div class="compliance-header">
                        <span class="compliance-title">Audit Regular</span>
                    </div>
                    <div class="compliance-details">Effectuer un audit de conformité mensuel pour vérifier la conformité continue.</div>
                </div>
                <div class="compliance-item">
                    <div class="compliance-header">
                        <span class="compliance-title">Mise à jour</span>
                    </div>
                    <div class="compliance-details">Garder le système à jour avec les derniers patches de sécurité.</div>
                </div>
                <div class="compliance-item">
                    <div class="compliance-header">
                        <span class="compliance-title">Monitoring</span>
                    </div>
                    <div class="compliance-details">Mettre en place une surveillance continue via les logs auditd et rsyslog.</div>
                </div>
            </div>
        </div>

        <!-- Footer -->
        <div class="footer">
            <p>Généré par: Ansible Audit Role | Date: {{ ansible_date_time.iso8601 }}</p>
            <p>⚠️ Ce rapport doit être gardé confidentiel</p>
        </div>
    </div>
</body>
</html>
